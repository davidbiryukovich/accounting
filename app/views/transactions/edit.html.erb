Edit Transaction form

<%= form_for @transaction, action: 'update', id: @transaction.id do |f| %>
  <%= render partial: 'form', locals: {f: f} %>
  <%= f.submit 'Update' %><br>
<% end %>

<%= link_to 'Back', action: params[:from], id: params[:id] %>