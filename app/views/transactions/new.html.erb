New Transaction form

<%= form_for @transaction, action: 'create' do |f| %>
  <%= render partial: 'form', locals: {f: f} %>
  <%= f.submit 'Create' %>

<% end %>

<%= link_to 'Back', action: 'index' %>
